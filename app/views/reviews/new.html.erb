
<div class = "event" data-controller="star-rating">
    <%= simple_form_for [@event, @review] do |f| %>
      <%= f.input :rating, collection: (1..5).to_a, label: "Rate your experience"%>
      <%= f.input :comment %>
          <%= f.submit "Submit", class: "btn" %>
    <% end %>
</div>
