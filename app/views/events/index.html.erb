
<div data-controller = "search">
  <% form_with url: events_path, data: { action = "submit->search#submit" } do |f| %>
  <% f.text_field :query, data: { target: "search.input" } %>
  <% f.submit "Submit" %>
<% end %>

  <ul data-search-target="list">
  </ul>
</div>

<div class = "event">
  <% @events.each do |x| %>
    <p id="name"><%= x.name %>
    <i class="fa-regular fa-bookmark"></i>
    <%= link_to "bookmark", event_bookmarks_path(x, @bookmark), data: {turbo_method: :post}%></p>
  <% end %>
</div>
